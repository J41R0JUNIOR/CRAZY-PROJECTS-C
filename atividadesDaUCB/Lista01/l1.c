#include <stdio.h>

int main(void){

  int a = 10, b = 15, c = 5, d = 3;
  int diferenca = a * b - c * d;

  printf("valor de %d * valor de %d - valor de %d * valor de %d", a, b, c, d);

  printf("\nProduto: %d", diferenca);
  

  return 0;
}
